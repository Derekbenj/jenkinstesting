import sklearn.linear_model as lm

def train_model(data):
    # """Trains a model (e.g., linear regression)."""
    # X = data[["feature1", "feature2"]]  # Features
    # y = data["target"]  # Target variable
    # model = lm.LinearRegression()
    # model.fit(X, y)
    # return model

    return data